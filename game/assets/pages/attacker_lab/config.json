{
    "_note": "Full instructor build of attacker/v0, submarine lesson. Filename stem ('attacker_lab') is this page's default save-slot key for saved input values, since save_slots.inputs is omitted below.",

    "build_type": "workspace",

    "settings": {
        "_ref": [
            "../../settings/_packages/_default.json",
            "../../settings/hack_settings/lab.json"
        ]
    },

    "inputs": {},

    "menu_bar": {
        "title": "attacker",
        "buttons": [
            { "_ref": "../../settings/layouts/menu_bar/admin.json" }
        ]
    },

    "panes": { "_ref": "../../settings/layouts/panes/attacker_suite.json" }
}
