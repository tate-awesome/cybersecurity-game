{
    "_note": "Full instructor build of attacker/v0, submarine lesson. Draft page.json format - see settings/pages/ discussion. Filename stem ('attacker_lab') is this page's default save-slot key for saved input values, since save_slots.inputs is omitted below.",

    "settings": {
        "_ref": [
            "/_packages/_default.json",
            "/hack_settings/lab.json"
        ]
    },

    "inputs": {},
    
    "menu_bar": {
        "title": "attacker",
        "buttons": [
            { "_ref": "layouts/menu_bar/admin.json" }
        ]
    },

    "panes": { "_ref": "layouts/panes/attacker_trifold.json" }
}
