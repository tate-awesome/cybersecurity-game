{
    "_note": "First lesson on the modbus_hvac layout - the house model paired with the lab_hvac settings package (HVAC registers).",

    "build_type": "workspace",

    "settings": {
        "_ref": ["_packages/_default.json", "modbus_settings/hvac.json"]
    },

    "menu_bar": {
        "title": "modbus_lab",
        "buttons": [
            { "_ref": "basic.json" }
        ]
    },

    "panes": { "_ref": "attacker_suite.json" }
}
