{
    "_note": "First lesson on the modbus_submarine layout - the submarine world-map model paired with the lab_submarine settings package (submarine registers).",

    "build_type": "workspace",

    "settings": {
        "_ref": ["_packages/_default.json", "modbus_settings/submarine.json"]
    },

    "menu_bar": {
        "title": "modbus_lab",
        "buttons": [
            { "_ref": "basic.json" }
        ]
    },

    "panes": { "_ref": "attacker_suite.json" }
}
