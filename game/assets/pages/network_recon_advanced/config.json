{
    "_note": "Second lesson on the same network_recon layout as network_recon_basics - distinguished only by its manifest lesson entry.",

    "build_type": "workspace",

    "settings": {
        "_ref": ["../../settings/_packages/lab.json"]
    },

    "menu_bar": {
        "title": "attacker",
        "buttons": [
            { "_ref": "../../settings/layouts/menu_bar/intro.json" }
        ]
    },

    "panes": { "_ref": "../../settings/layouts/panes/network_recon.json" }
}
